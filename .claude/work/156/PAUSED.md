# #156 — paused → RESUMED 2026-05-14

> **Resume note (2026-05-14):** `@hamak/*` 0.5.5 published with the ESM fix. `npm update` pulled
> `microkernel-impl`, `notification`, `logging` (and others) from 0.5.2 → 0.5.5. The previously
> blocked Host-bootstrap test now passes 3/3; full Vitest suite passes 184/184; `vite build` clean.
> Pipeline resumed and routed to code-reviewer. Sections below are kept as the historical pause
> record.

---

**Pause date:** 2026-05-13
**Blocked on:** [`amah/app-framework#11`](https://github.com/amah/app-framework/issues/11) — extensionless ESM imports across 14/15 `@hamak/*` packages break Vitest's `Host` bootstrap.
**Reason for pause:** the spec's Acceptance #6/#7 require a real `Host([loggingPlugin, notificationPlugin]).bootstrapAllAtRoot()` Vitest test. That test can't load `@hamak/microkernel-impl` under Vitest 1.6.1 + Node 22 strict ESM until the framework republishes `dist/` with `.js` extensions on its relative re-exports. No in-scope `vite.config.ts` knob lets us work around it (confirmed by dev cycle 2).

## What's in the worktree

**Production code from dev cycle 1 (kept):**
- `frontend/src/plugins/logging/loggingPlugin.ts` — new thin adapter onto `@hamak/logging`
- `frontend/src/plugins/notification/notificationPlugin.ts` — rewritten as thin delegator to `@hamak/notification` factory
- `frontend/src/kernel/bootstrap.ts` — registers logging before notification; `dependsOn: ['store', 'logging']`
- `frontend/package.json` — `@hamak/logging` promoted to direct dep
- `CLAUDE.md:121-122` — stale `@hamak/notification-api`/`@hamak/notification-impl` replaced with single-package + new `@hamak/logging` line
- `frontend/package-lock.json` — regenerated by `npm install`
- `frontend/vite.config.ts` — `test.server.deps.inline: [/^@hamak\//]` block (no-op today; may help once upstream #11 lands)

**Test code (kept):**
- `frontend/src/plugins/notification/__tests__/spec-grep-guards.test.ts` — 7 passing content-guard tests for criteria #1–#4, #8, #9, #10
- `frontend/src/plugins/notification/__tests__/notificationPlugin.test.ts` — 3 tests covering #6, #7 that won't run until #11 ships

**Untracked / orchestrator artifacts (not for commit):**
- `.claude/work/156/` — full agent pipeline history

## Pipeline state at pause

| Stage | Cycle | Verdict | Artifact |
|---|---|---|---|
| spec-writer | 2/3 | done | `spec.md` |
| spec-reviewer | 2/3 | **approve** | `spec-review-cycle-2.md` |
| developer | 2/3 | **escalated** | `dev-escalation.md` |
| test-author | 1/3 | 7/10 pass + 3 fail (blocked on upstream) | `test-results.md` |
| rework-coordinator | 1 | route-to-dev (resolved → escalated) | `next-step.md` |
| code-reviewer | — | not run | — |
| PR | — | not opened | — |

## When upstream #11 ships — resume checklist

1. `npm install` in `frontend/` to pull the fixed `@hamak/*` packages.
2. `cd frontend && npx vitest run src/plugins/notification/__tests__/notificationPlugin.test.ts` — expect 3 tests to pass (the `vite.config.ts` `server.deps.inline` block was left in place for this).
3. If 3/3 green: spec-writer cycle 3 to re-affirm scope (no spec content change, just close the loop), then code-reviewer, then PR.
4. If still failing: re-open the escalation; investigate `test.deps.optimizer.web.include` as the next knob. Likely indicates upstream fix was partial.

## Related upstream filings (both still open at pause)

- `amah/app-framework#10` — hidden Redux coupling in notification plugin
- `amah/app-framework#11` — extensionless ESM imports (the pause blocker)

## Filed but not committed

Per working rules, nothing in `.claude/` has been committed. The production worktree diff is also uncommitted (no branch was cut). When resuming, the next action is to either commit the cycle-1 production changes to a branch and continue from test-stage, or to wait for upstream and merge fresh.
